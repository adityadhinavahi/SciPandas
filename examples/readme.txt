Readme 
-----------------------------------------------------------------
To call any example in this directory:
Load the SciPandas Library
Just execute exec('path_to_examples/example_file.sce')
where path_to_examples is the path of the example directory and example_file is any file in the examples directory
